{
  "name": "simple-continuation-local-storage",
  "version": "1.0.3",
  "description": "Simple continuation local storage for Node > 6",
  "main": "index.js",
  "scripts": {
    "test": "mocha --exit *.test.js",
    "push": "message=\"${1:-build}\" && versiony ./package.json --patch && npm publish && git add . && git commit -am \"$message\" && git push"
  },
  "author": "Mike Talbot",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^7.2.0",
    "versiony-cli": "^1.3.0"
  }
}