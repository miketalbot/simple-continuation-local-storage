{
  "name": "simple-continuation-local-storage",
  "version": "1.0.12",
  "description": "Simple continuation local storage for Node > 6",
  "main": "index.js",
  "scripts": {
    "test": "mocha --exit *.test.js",
    "push": "versiony ./package.json --patch && npm publish && git add . && git commit -am build && git push"
  },
  "author": "Mike Talbot",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^7.2.0",
    "versiony-cli": "^1.3.0"
  }
}